/// Small text size used for compact labels and dense interface hints.
const textSizeSmall = 8.0;

/// Default body text size used in most compact UI surfaces.
const textSizeMedium = 12.0;

/// Large text size used for section headings and important labels.
const textSizeLarge = 20.0;

/// Extra-large text size used for prominent display text.
const textSizeExtraLarge = 32.0;
